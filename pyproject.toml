[project]
name = "ai-news-aggregator"
version = "0.1.0"
description = "AI-powered news aggregation system that curates AI developments from RSS feeds and blogs"
readme = "Readme.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.18.5",
    "apscheduler>=3.11.3",
    "beautifulsoup4>=4.12",
    "fastapi>=0.139.2",
    "feedparser>=6.0",
    "google-genai>=1.0",
    "httpx>=0.27",
    "jinja2>=3.1.6",
    "psycopg2-binary>=2.9",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.4",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0",
    "trafilatura>=2.0",
    "uvicorn[standard]>=0.51.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[project.scripts]
ai-news = "app.pipeline.daily:run_daily_pipeline"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app", "agent"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
